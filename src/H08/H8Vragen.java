package H08;

public class H8Vragen {
// Vraag 1 Wat is event-handling?: Dat regelt wat er daadwerkelijk moet gebeuren, Daarna regelt hij ook precies watg er gebeurt.
// Vraag 2 Wat is een gebruiksinterface? Manier waarop de gebruiker met een computersysteem communiceert.
// Vraag 3 Waarom moet er voor een knop wel en voor een tekst geen event-handling geprogrammeerd worden?
    //Omdat als je op een knop drukt, moet er een handeling plaatsvinden. als je een tekst typt is er nog geen handeling plaatsgevonden alleen dat de tekst verandert dan.
    // Vraag 4 Wat is een lege string: een lege string is een tekst waar niks in staat.
    //Vraag 5 Wat is het verschil tussen een tekstvak en een label? een Label is alleen een stuk tekst, terwijl een tekstvak een vak is waar tekst in komt te staan.
    //Vraag 6 Waarom moeten getallen uit een tekstvak altijd geconverteerd worden naar een double of een int?
    // Omdat je met double een komma getal kunt converteren en met int krijgt het getal een waarde.
}
